# Machine-independent local dev settings
# Copy this file to .env and adjust paths if your repo layout differs.

# Relative to the frontend project root (this .env lives here)
FRONTEND_DIR=.

# Path to the Spring Boot backend project
# Default assumes monorepo layout: frontend/../backend/hda-back
BACKEND_DIR=../../backend/hda-back

# Ports (override if the defaults are busy on your machine)
PORT_PROXY=8080
PORT_SPRING=8081
PORT_UI=3000
